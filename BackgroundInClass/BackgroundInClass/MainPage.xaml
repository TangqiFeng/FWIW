<Page
    x:Class="BackgroundInClass.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BackgroundInClass"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel Margin="10, 150, 10, 0">
            <TextBlock x:Name="tblLat" Text="Latitude: "/>
            <TextBlock x:Name="tblLong" Text="Longitude: "/>
        </StackPanel>

        <Button x:Name="btnRegister" Content="Register" Margin="10, 250, 10, 0" 
                Click="btnRegister_Click"/>
        <TextBlock x:Name="tblStatus" Text="Status" Margin="10, 350, 10, 0" Height="50" TextWrapping="Wrap"  />
    </Grid>
</Page>
